# Havit 项目进度计划表

更新时间：2026-09-07

本文档用于回答三个问题：

1. 现在项目做到哪里了。
2. 功能应该怎么分区、怎么判断完成。
3. 接下来应该先向哪个方向发力。

进度判断依据：`docs/havit-product-design.md`、`docs/havit-tech-architecture.md`、当前前后端路由/API/服务/测试文件。百分比是产品可交付口径的估算，不是代码行数统计。

## 一、当前结论

项目已经不是概念阶段，也不只是骨架阶段。当前更准确的状态是：

| 层级 | 当前判断 | 完成度估算 | 说明 |
|---|---:|---:|---|
| M1 / P0 核心主链路 | 已完成内测基线 | 95% | 迁移风险、默认 E2E、Docker 验证、分类口径均已收口；后续只保留常规回归、权限和移动端深验收 |
| M2 / P1 核心差异化 | S2 收束完成：10 件事 1-10 全部收口 | 82% | 快速录入三入口（拍照/条码/手动）异常降级已打穿并补测试；搜索闭环含缩略图与归档开关；低库存/寿命到期自动进入提醒并可通知；出借遗失沉淀异常模块与生命周期日志；备份包可还原（自动化测试 + 二进制级演练）；README 阶段对齐 |
| M3 / P2 体验深化 | 已提前做了一部分，但不应继续扩张 | 40% | 虚拟资产、容器、理赔 PDF、PAT、EDC 打包等已出现；会话面板、确权变更、家庭内部借用、离线写仍未成型 |
| 发布信心 | 可以进入“内测稳定化”，不建议直接当正式版 | 72% | P0 验收骨架已经建立，S2 已开始补真实工作流；正式发布前还需要移动端一致性、隐私权限、异常路径、通知/备份实机验证 |

当前最重要的方向：S2 的 10 件事已全部收口（快速录入、搜索定位、消耗品+提醒、借出+异常、备份恢复演练、README 阶段更新）。S2 告一段落，进入 **S3 家庭与运维能力加固**：多用户权限验收、隐私隔离、设置页 E2E、通知真实 endpoint、恢复演练（已做）、安全检查清单。仍不要横向铺开新功能。

## 二、本次复核结论

复核范围：关键提交 `fa519133`、`57d9b22c`、`4bd6ed6b`，以及当前工作区 S2 第一切片和测试命令。

| 项目 | 结论 | 证据 |
|---|---|---|
| 迁移审计 | 通过 | `fa519133` 删除了 `0001_init.sql` 中 `Down` 段混入的旧建表 SQL，并新增 `internal/db/migration_test.go` 验证回滚后核心表被移除 |
| Go 基线 | 通过 | 临时设置 `GOSUMDB=sum.golang.org` 后，`go test ./...` 通过 |
| 前端构建 | 通过 | `pnpm build` 通过，产物正常生成到 `internal/static/dist` |
| P0 E2E 主体 | 通过 | 2026-07-04 默认 `pnpm test:e2e` 通过 22 条；Playwright 已向 Vite dev server 传递 `HAVIT_E2E_FRONTEND_PORT` |
| Docker 验证脚本 | 通过 | 2026-07-04 Docker Desktop 4.77.0 / Engine 29.5.3 环境下 `scripts/verify-docker.ps1` 通过，覆盖镜像构建、clean release setup、重启后持久化 |
| 分类系统口径 | 已决策并改文档 | `is_system` 已废弃，不存在只读默认分类；迁移里的初始分类只是可编辑 starter taxonomy，`CategoryService` 继续允许改/删 |
| S2 快速录入第一切片 | 已启动并通过基线 | 新增无 item 依赖的拍照草稿识别；`CaptureDesktop` 支持 AI/条码失败转手动、必选位置保存、保存后照片挂到真实物品；默认 E2E 增至 23 条 |

结论：S0、S1、S1.1 已完成。现在正式进入 S2，但 S2 不能按模块横向铺开，要按真实工作流纵向收束。当前唯一主线是快速录入闭环。

## 三、状态标记

| 状态 | 含义 |
|---|---|
| 完成 | 数据模型、API、UI、权限、测试、文档都达到可验收状态 |
| 可用待验收 | 主流程已通，但测试、异常路径、移动端或权限仍需补齐 |
| 进行中 | 有部分模型/API/UI，但链路不完整 |
| 待开始 | 仅在产品文档中规划，代码中没有可靠实现 |
| 后置 | 明确不影响当前 MVP，可等稳定后再做 |

## 四、功能分区与当前进度

| 分区 | 目标能力 | 当前状态 | 完成度 | 当前证据 | 下一步控制点 |
|---|---|---:|---:|---|---|
| 基础设施与部署 | 单二进制、Docker、自部署、静态前端嵌入 | 可用待验收 | 92% | `cmd/havit/main.go`、`Dockerfile`、`compose.yaml`、`scripts/verify-docker.ps1`、Docker 验证通过记录 | 发布前继续用脚本做回归；后续可接 CI |
| 配置系统 | Viper、DB 动态配置、环境变量锁定、热重载 | 可用待验收 | 75% | `internal/config`、`SettingsDesktop`、`system_configs` | 校验环境变量锁定 UI、敏感字段脱敏、通知/AI 配置实时生效 |
| 认证与初始化 | release/demo 模式、首次 setup、JWT、Owner | 可用待验收 | 92% | `AuthService`、`SystemState`、`README.md`、auth tests、Docker 验证脚本 | P0 基线已够；下一步转向 Owner/Member 权限验收 |
| 用户与角色 | Owner/Member、成员管理、角色变更 | 进行中 | 55% | `UserHandler`、`usersApi`、设置页用户管理 | 做权限验收：Owner-only 路由、成员不可改实例配置、删除用户约束 |
| 隐私隔离 | 私有资产、私有位置、位置树继承、管理员不穿透 | 进行中 | 40% | 表字段已有 `is_private`、`owner_id` | 逐接口审计过滤逻辑；补搜索、位置树、导出、详情页隐私测试 |
| 核心物品台账 | 物品 CRUD、详情页、状态、照片、备注、元数据 | 可用待验收 | 78% | `ItemService`、`ItemHandler`、`AssetsDesktop/Mobile`、`ItemDetail` | 统一状态流转口径；补照片/附件 E2E；校准详情页移动端能力 |
| 位置管理 | 树形位置 CRUD、语义类型、路径展示 | 可用待验收 | 82% | `LocationService`、`LocationsDesktop/Mobile`、locations E2E | 验证删除约束、私有节点继承、深层路径性能 |
| 容器型物品 | 物品作为容器，内部物品联动 | 进行中 | 45% | `containerApi`、`parent_item_id`、详情页容器面板 | 明确容器移动是否自动影响子物品路径；补 API 和 UI 验收 |
| 分类系统 | 可编辑初始分类、自建分类、动态 Tab | 可用待验收 | 74% | `categories` 表、`CategoriesDesktop`、`CategoryTabs`、P0 API E2E | `is_system` 已废弃，不存在只读默认分类；后续聚焦排序、图标和批量整理体验 |
| 标签系统 | 标签 CRUD、物品打标、过滤 | 可用待验收 | 80% | `TagService`、`TagsDesktop`、`replaceTags` | 补标签筛选 E2E；确认重复名、颜色格式和删除约束 |
| 手动录入 | 名称 + 位置快速创建，后续补充 | 可用待验收 | 88% | `itemsApi.create`、资产页新增表单、`CaptureDesktop`、`web/e2e/capture.spec.ts` | 补移动端快速录入；确认保存后跳转详情还是资产列表 |
| 批量导入 | CSV/JSON 导入、位置路径自动创建、错误报告 | 可用待验收 | 78% | `ImportService`、`ImportDesktop`、import tests、P0 API E2E | 产品规划中的字段映射向导尚未完整；补 UI 级导入 E2E 和示例模板 |
| 导出 | CSV/JSON 手动导出 | 可用待验收 | 76% | `ExportService`、`exportApi`、操作页、P0 API E2E | 继续验证导出字段完整性、隐私过滤和大数据量表现 |
| 条码录入 | 摄像头扫码、条码查询、失败降级 | 进行中 | 78% | `BarcodeService`、`CaptureDesktop` 条码区（输入+扫码+命中自动填充）、查不到/接口异常降级 E2E | 真实摄像头扫码人工验收 |
| AI 拍照识别 | 上传照片、结构化识别、原图存证、元数据提炼 | 进行中 | 70% | `AIRecognitionService`、`POST /ai/recognize-photo`、`OpenAIProvider`、provider nil/error/success 测试、存证语义测试与 E2E | 元数据自动提炼；移动端真机验收 |
| 搜索与定位 | FTS 即时搜索、SSE、自然语言增强、结果上下文 | 可用待验收 | 85% | `SearchService`（FTS 转义+LIKE 合并、标签/位置命中、借出提示、缩略图、归档显示开关）、`SearchDesktop`（search_error 处理、封面缩略图）、8 条搜索 E2E | 自然语言解析结果的手工验收；多图物品缩略图一致性 |
| 消耗品 A：免数日耗 | 购买事件、校准事件、补货预测 | 可用待验收 | 72% | `purchase_events`、`calibration_events`、`SuppliesDesktop` | 检查预测算法准确性；补“快没了/还有很多”影响下一次预测的测试 |
| 消耗品 B：计件备品 | 当前库存、一键使用、阈值、寿命倒计时 | 可用待验收 | 80% | `use-one`、`current_stock`、`lifespan_days` | 低库存/寿命已联动提醒（`stock_low`/`filter_life` 自动创建、补货自动关闭）；真机验收移动端 |
| EDC 随身常备 | 基准位置、动态状态、打包/归位 | 进行中 | 62% | `EssentialsDesktop`、`packAll`、`returnAll` | 补搜索降级提示；移动端出门清单应作为高优先级验收 |
| 二维码位置 | 位置码生成、打印、扫码反查 | 可用待验收 | 76% | `generateQRCode`、`QrPrintDesktop`、`LocationScanDesktop`、locations E2E | 用真实打印/扫码流程验收；手动输入路径已有 E2E 覆盖 |
| 借出追踪 | 借出、归还、逾期、出借遗失责任交割 | 可用待验收 | 74% | `LoanService`、`LoansDesktop`、`markUnreturned` | 出借遗失已联动异常模块与生命周期日志（含 UI 入口）；真机验收移动端 |
| 凭证与保修 | 附件、保修字段、到期列表、凭证视图 | 进行中 | 58% | `AttachmentService`、`CredentialsDesktop`、`warranty` API | 补 PDF/图片预览体验；验证保修提醒生成与通知 |
| 资产退场 | 闲置、售出、赠出、报废、丢失、归档 | 进行中 | 66% | `exit` API、`graveyard`、状态字段 | 需要统一状态机文档；归档显示开关已打通（搜索按用户偏好展示归档物品） |
| 异常资产 | 丢失、被盗、损坏、处理进度、损耗记录 | 可用待验收 | 72% | `AbnormalService`、`AbnormalDesktop`、`lossRecords` | 借出遗失自动进入异常（Lent & Lost，upsert）；恢复/结案路径与统计口径待补 |
| 理赔 PDF | 被盗/理赔凭证导出 | 进行中 | 55% | `claim_pdf.go`、`claimPdf` API | 用真实附件和中文字段渲染验收 PDF；补视觉回归或快照测试 |
| 虚拟资产 | 买断资产、平台凭证、增补购买 | 进行中 | 58% | `VirtualAssetService`、`VirtualAssetsDesktop`、`virtual_credentials` | 明确虚拟资产录入入口；验证授权码加密、导出脱敏 |
| 提醒系统 | 保修、借出、库存、寿命提醒 | 进行中 | 68% | `ReminderService`、`NotifyService`、`reminders` 表 | 保修/库存/寿命提醒已由业务流自动创建；借出逾期提醒联动待验收 |
| 通知网关 | Webhook、Ntfy、Apprise、调度器 | 可用待验收 | 65% | `HTTPNotifyGateway`、设置项、notify tests | 做真实 endpoint 测试；增加失败重试/错误可见性策略 |
| 备份恢复 | 定时备份、手动备份、附件打包、恢复演练 | 可用待验收 | 78% | `BackupService`、backup tests（含 `TestBackupRestoresIntoFreshDataDir`）、`docs/backup-restore.md` | 恢复演练文档已补齐，2026-09-07 二进制级演练通过（中文物品/附件/FTS 全还原） |
| PAT 开放生态 | 个人访问令牌创建、撤销、最后使用 | 可用待验收 | 68% | `APITokenService`、`SettingsDesktop`、`api_tokens` | 补 token 过期、last_used、撤销后鉴权失败测试 |
| 设备与会话 | 登录设备列表、强制下线单设备 | 待开始 | 20% | 只有 `token_version` 可全部失效 | 若当前不做原生多端，可后置到 M3；不要阻塞 MVP |
| PWA 与移动端 | PWA manifest、网络状态、移动端核心页 | 进行中 | 60% | `manifest.json`、`useNetworkStatus`、MobileShell、部分 Mobile 页 | 先验收资产、位置、消耗品、详情四个移动主链路 |
| 测试体系 | Go 单测、前端 build、E2E、部署验收 | 进行中 | 83% | `go test ./...`、`pnpm build`、38 条 Playwright（条码异常路径 + 搜索闭环 + 消耗品/借出联动）、迁移回滚测试、Docker 验证脚本通过 | 每个 S2 工作流至少补 1 条关键 E2E |

## 五、里程碑计划

### S0：基线整理与风险清理（已完成）

完成时间：2026-07-03

| 目标 | 验收标准 | 产出 |
|---|---|---|
| 建立可重复验证基线 | `go test ./...`、`pnpm build`、现有 Playwright 全部通过 | Go 与前端构建通过；默认 Playwright 命令已在 S1.1 收口 |
| 清理迁移风险 | `internal/db/migrations/0001_init.sql` 的 `Down` 段确认无误 | 已删除旧建表残留，并新增迁移回滚测试 |
| 明确功能状态 | 按本文档表格更新每个模块状态 | 已更新为 2026-07-04 S2 启动版 |
| 统一术语 | UI 中产品命名与数据库标识映射一致 | S1.1 已决策：分类无只读默认预设，`is_system` 为废弃兼容字段 |

S0 结论：迁移高风险已关闭，测试基线已建立。需要注意的是本机默认 `GOSUMDB=off` 会阻止 Go toolchain 校验，复核时使用 `GOSUMDB=sum.golang.org` 后测试通过。

### S1：M1 / P0 MVP 验收（已完成）

完成判断：可以称为“MVP 可内测”，并作为后续 S2 工作流收束的稳定基线。

| 主链路 | 必须做到 | 验收方式 |
|---|---|---|
| clean install | 空库 release 启动 -> `/setup` -> Owner 创建 -> 登录 | Docker 验证脚本已覆盖并通过 |
| 资产录入 | 手动新增物品，选择分类、位置、备注，能在列表和详情看到 | Playwright UI 已覆盖新增耐用资产 |
| 位置管理 | 新建多级位置，物品挂载后显示完整路径 | Playwright UI 已覆盖 |
| 分类/标签 | 分类/标签 CRUD、物品打标、筛选 | P0 API E2E 已覆盖 |
| 导入导出 | CSV/JSON 导入后可搜索、可导出 | P0 API E2E 已覆盖 JSON 导入与导出 |
| 归档 | 删除物品实际归档，不硬删除，主列表默认不显示 | P0 API E2E 已覆盖 |
| Docker | 前端构建嵌入后 Docker 启动可访问 | `scripts/verify-docker.ps1` 已通过，覆盖 clean release setup 和重启持久化 |

S1 结论：核心链路已可内测；不要再扩 P0 功能面，后续只做回归和缺口修补。

### S1.1：进入 S2 前的短收口（已完成）

完成时间：2026-07-04

| 收口项 | 原因 | 完成判定 |
|---|---|---|
| 修默认 E2E 端口配置 | 默认 `pnpm test:e2e` 等 5174，但 `pnpm dev` 默认 5173 | 已完成：不加环境变量直接跑通 `pnpm test:e2e`，22 条通过 |
| 跑 Docker 验证脚本并记录结果 | 自部署是核心承诺，脚本存在不等于实机通过 | 已完成：Docker daemon 可用环境下 `scripts/verify-docker.ps1` 通过 |
| 决策并修正默认分类只读口径 | `is_system` 已废弃，不存在只读默认分类 | 已完成：产品/技术文档明确初始分类可编辑、可删除 |

### S2：M2 高频日常工作流收束（进行中）

执行窗口：2026-07-04 至 2026-07-19

| 工作流 | 要打穿的用户故事 | 验收重点 |
|---|---|---|
| 快速录入 | 用户拍照/扫码/手动三选一，失败时不阻塞录入 | AI/条码失败降级、原图存证、手动兜底保存 |
| 找东西 | 搜索物品、标签、位置、EDC 状态，结果给出下一步行动 | SSE、FTS 中文、EDC 降级提示、移动端可用 |
| 消耗品 | 咖啡豆记录购买与校准，滤芯一键使用并触发低库存 | 预测、阈值、提醒 |
| 随身常备 | 出门前一键打包，回家一键归位 | 移动端优先 |
| 二维码 | 打印位置码，扫码反查容器内容 | 真实浏览器/摄像头 |
| 借出 | 借给他人、到期提醒、归还或出借遗失 | 状态联动、事件日志 |
| 保修凭证 | 上传凭证，保修到期进入提醒，凭证可查 | 附件、提醒、聚合页 |

当前 S2 切片进度：

| 切片 | 状态 | 已完成 | 下一步 |
|---|---|---|---|
| S2.1 快速录入闭环 | 已收口 | 拍照草稿识别；条码录入（输入+扫码，命中自动填充，查不到/接口异常降级手动且可保存）；AI/条码失败进入手动确认；保存必选位置；保存后照片挂到真实物品；AI provider nil/error/success 后端测试；E2E 28 条 | 真机验收移动端拍照/扫码 |
| S2.2 搜索与定位闭环 | 已收口 | FTS 特殊字符防错 + LIKE 合并去重；2 字中文词 LIKE 兜底；标签/位置名命中；EDC 降级提示；借出物品状态提示（含逾期）；search_error 前端提示；搜索结果缩略图；主搜索归档显示开关（用户偏好）；8 条 E2E | 进入消耗品 + 提醒联动 |

S2 完成后，项目的核心差异化才算真正可体验。当前不要把精力分散到新模块，先把 S2.1 做到失败也能保存、保存后可追溯、手机上可用。

### S3：家庭与运维能力加固

建议窗口：2026-07-25 至 2026-08-07

| 领域 | 必须回答的问题 | 验收重点 |
|---|---|---|
| 多用户 | Owner 和 Member 的边界是否真的生效 | Owner-only API、成员不可越权 |
| 隐私 | 私有资产、私有位置是否在列表、搜索、导出中彻底不可见 | 跨账号测试 |
| 设置 | AI、通知、偏好、分类、用户管理是否都能在 UI 中修改并即时生效 | 设置页 E2E |
| 通知 | Webhook/Ntfy/Apprise 是否能真实发出，失败是否可见 | 真实 endpoint 测试 |
| 备份 | 备份文件是否包含 DB 和附件，是否可恢复 | 恢复演练 |
| 安全 | PAT、JWT、密码、license key、导出文件是否满足最小泄漏面 | 安全检查清单 |

S3 完成后，项目才适合作为家庭生产环境长期试用。

### S4：M3 已提前实现功能的产品化

建议窗口：2026-08-08 至 2026-08-21

| 功能 | 当前建议 | 不做什么 |
|---|---|---|
| 虚拟资产 | 先做买断资产台账、凭证、增补购买闭环 | 不做订阅管理 |
| 容器型物品 | 明确容器移动联动规则，并补测试 | 不做平面地图 |
| 理赔 PDF | 只围绕被盗/损坏凭证导出验收 | 不做复杂财务报表 |
| PAT | 做好自动化脚本接入文档和撤销语义 | 不急着做 OAuth |
| 会话管理 | 若无明确多端痛点，保持后置 | 不阻塞 MVP |
| 离线写 | M3 后再评估 IndexedDB mutation queue | 当前只做离线读和写按钮置灰 |

## 六、当前最该发力的 10 件事

按顺序做，不建议跳过。

| 排名 | 任务 | 原因 | 完成判定 |
|---:|---|---|---|
| 1 | 补快速录入异常路径 E2E | S2.1 的价值不在 happy path，而在 AI/条码失败时不阻塞用户 | 条码查不到、条码接口异常、AI fallback 都能进入手动确认并保存 |
| 2 | 验收拍照识别存证语义 | 现在草稿识别和保存后附件入库是两段式，必须明确不会丢原图 | 草稿阶段不写 item；保存后照片成为真实物品附件，失败时退回普通上传 |
| 3 | 补 AI provider 成功/失败后端测试 | 识别依赖外部服务，失败策略不能只靠 UI 兜底 | provider nil、provider error、provider success 都有测试覆盖 |
| 4 | 验收快速录入移动端入口 | PWA 的录入场景主要发生在手机上 | 手机视口下拍照、手动确认、选择位置、保存全流程可用 |
| 5 | 搜索与定位闭环 | “找得到东西”是产品核心价值 | 关键词、标签、位置、EDC 状态都有可验收路径 |
| 6 | 移动端高频页面收束 | PWA 用户主要在手机上查找和录入 | 资产、位置、详情、消耗品移动端可用 |
| 7 | 消耗品 + 提醒联动 | 消耗品价值依赖提醒闭环 | 低库存/寿命到期能进入提醒并可通知 |
| 8 | 借出 + 异常联动 | 借出遗失必须沉淀到生命周期日志 | 出借遗失进入异常状态和事件日志 |
| 9 | 备份恢复演练 | 自部署用户最怕数据丢 | 备份包可在新数据目录恢复 |
| 10 | README 阶段更新 | README 仍容易让人误判项目停在 M1 第二轮 | README 与 PROJECT.md 的阶段一致 |

## 七、完成定义

一个功能不能只因为“有表、有 API、有页面”就算完成。建议以后统一使用下面的完成定义：

| 检查项 | 要求 |
|---|---|
| 数据模型 | 字段完整，迁移可重复执行，索引与约束明确 |
| 后端 API | 正常路径、错误路径、权限边界都有测试 |
| 前端 UI | 桌面端和目标移动端都能完成主流程 |
| 离线/网络 | 网络断开时写操作有明确禁用或错误提示 |
| 隐私权限 | Member、Owner、私有资产、私有位置均有验收 |
| 可观测错误 | 用户知道失败原因，开发者能定位问题 |
| 文档 | README 或对应文档说明如何使用和验收 |
| E2E | 至少覆盖一条真实用户路径 |

只有以上全部满足，状态才从“可用待验收”改为“完成”。

## 八、每周进度控制节奏

每周固定做一次 30 分钟项目盘点，按下面顺序更新本文档：

| 步骤 | 内容 | 输出 |
|---|---|---|
| 1 | 跑基线命令 | 记录通过/失败 |
| 2 | 更新功能表状态 | 只在验收通过后上调完成度 |
| 3 | 检查上周新增风险 | 进入风险表，不在脑子里记 |
| 4 | 选本周最多 3 个目标 | 防止同时推进太多模块 |
| 5 | 为每个目标写验收标准 | 没有验收标准的目标不开始 |

建议每周目标数量上限：

| 类型 | 上限 |
|---|---:|
| 主功能收束 | 1 个 |
| E2E/测试补齐 | 1 个 |
| 风险/债务处理 | 1 个 |

## 九、风险清单

| 风险 | 影响 | 当前等级 | 控制办法 |
|---|---|---:|---|
| 功能铺得太快 | 进度看起来高，但主链路不可发布 | 高 | 以 S2 工作流验收为准，暂停新功能扩张 |
| 隐私隔离未逐接口验证 | 违背家庭私有资产承诺 | 高 | 写跨账号集成测试 |
| E2E 覆盖仍偏 P0 | UI 回归难以及时发现，P1 工作流没有防线 | 中 | 每个 S2 工作流至少补 1 条关键 E2E |
| 快速录入异常路径还不够厚 | S2.1 可能只在手动 happy path 稳，AI/条码失败仍有回归风险 | 中 | 先补失败路径 E2E，再推进搜索与定位 |
| 拍照草稿识别和原图存证是两段式 | 草稿识别不落库，保存时再挂附件；若语义不清，容易被误判为丢图或重复识别 | 中 | 文档和测试明确：草稿不入库，保存后真实物品必须有照片附件 |
| AI/条码依赖外部服务 | 录入体验不稳定 | 中 | 降级到手动录入，失败可继续保存 |
| 通知只在代码层可用 | 用户以为有提醒，实际收不到 | 中 | 做真实 endpoint 验收和错误展示 |
| 移动端页面覆盖不均 | PWA 核心场景受损 | 中 | 优先移动端查找、录入、详情、消耗品 |

已关闭风险：

| 风险 | 关闭依据 |
|---|---|
| 数据库迁移 Down 段疑似混入旧建表 | `fa519133` 删除残留 SQL，并新增 `TestInitialMigrationDownRemovesSchema` |
| 默认 `pnpm test:e2e` 端口配置不闭合 | Playwright 前端 webServer 显式传递 `HAVIT_E2E_FRONTEND_PORT`，默认命令已通过 |
| Docker 验证依赖本机 daemon | 2026-07-04 已在 Docker Desktop 可用环境跑通并记录 |
| 分类历史字段口径不清 | 产品/技术文档明确 `is_system` 废弃，不存在只读默认分类 |
| README 与实际阶段不一致 | 2026-09-07 README 阶段描述已与 PROJECT.md 对齐（M2 收束 + 备份恢复文档入口） |

## 十、建议的当前版本定位

当前版本建议称为：

> M2 收束启动：P0 已达到内测基线，P1/P2 多模块已铺开，正在把高频工作流逐个产品化。

不建议称为：

- 正式版
- P1 完成
- M3 开发期

原因是：P0 主链路已经能跑，验收体系也开始建立；但 P1 高频体验、移动端一致性、隐私权限、通知备份实机验证还没稳。下一阶段的成功不取决于再加多少功能，而取决于能否让现有功能在真实家庭资产管理场景里连续、可靠、可恢复地跑起来。
